# ASOUD audit — 2026-09-19

Scope: both current working trees, including untracked files. HEAD dates are September 12; filesystem timestamps cannot establish authorship. Findings below refer to **pre-fix line numbers**. Screenshots supplied in the conversation cover the personnel list/detail only; no reference screenshots for access, contracts or generic requests were supplied.

| Severity | Original location | Problem and user impact | Recommended correction |
|---|---|---|---|
| Critical | backend `asoud_workflow_request.json:23` | Employee has unrestricted DocType read/write/create. Generic REST/list/File access can bypass the custom owner check and expose or alter others' requests. | Owner-only read, no generic mutation; backend permission hooks and private File tests. |
| High | backend `asoud_party_profile.json:52` | Salary fields have permlevel 0; Accounts users can read them through standard REST. | HR-only field permission level; controller validation; strip data from custom serializers and shared caches. |
| High | backend `auth.py:115,193` | Access management lacks company checks and protected-account checks; removal disables a global User rather than office access. | Validate company, employee/user uniqueness, scope and sensitive roles; unlink without disabling unrelated login. |
| High | Flutter `generic_request_page.dart:68` | Missing class closing brace prevents compilation; whole-app analyzer reports 68 diagnostics. | Repair and run whole-app analysis. |
| High | backend `workflow_request.py:64,113` | No idempotency; request is not linked to instance, values do not seed tasks/conditions, Employee cannot insert native instance. | Stable request key, transactional create, explicit request-target workflows, reference and initial response wiring. |
| High | Flutter `generic_request_page.dart:19,97` | Direct online calls; no durable queue/cache/details/error handling; retries can duplicate data. | Scoped offline repository, durable payloads and retry, explicit failed/pending states, detail page. |
| High | backend `auth.py:73`; Flutter `personnel_roles_page.dart:28,277`, `personnel_design.dart:413` | Module labels do not match accepted roles, matrix is not enforced, all rights initially selected, edit callback missing. | Use supported canonical roles and persist edits; reject/hide unimplemented fine-grained policy instead of claiming enforcement. |
| High | backend `auth.py:163` | Invitations marked Sent without transport; SMS/WhatsApp have no implementation; no idempotency. | Email queue and truthful status; stable key; reject unavailable transports. Never send live invitations during tests. |
| Medium | Flutter `personnel_forms.dart:597,1069` | Salary fields absent from form groups; freshly loaded data ignored in category pages. | Render actual fields, validate finite amounts, use refreshed server profile. |
| Medium | Flutter `personnel_forms.dart:659` | Every document treated as an active contract; employment status inferred only from Party disabled flag. | Explicitly report attachments and unknown contract status; use native employment status. |
| Medium | backend `workflow_response.py:25,36`; Flutter `generic_request_page.dart:242` | Non-finite numeric values accepted; attachment contract expects public files; checkbox state and date/attachment controls incomplete. | Strict field validation, private authorized attachments, typed form controls. |
| Medium | Flutter `personnel_repository.dart:269` | Sensitive HR values copied to a shared party cache without user scoping. | Keep financial values only in session-scoped HR cache; never overlay pending financial changes after permission loss. |
| Low | Flutter `personnel_design.dart:389,449`; `personnel_roles_page.dart:333,412` | Endless loading on errors, inert tabs, unused classes and fake default person identity. | Error/retry UI, remove dead controls/default data. |

The workflow stage editor writes `form_fields` and the backend normalizer persists them. This is not treated as broken merely because tests were missing; a real round-trip test is required. Preserve graph/designer source hashes throughout the audit.

Advanced per-action permission management, SMS/WhatsApp delivery and a structured employment contract lifecycle must not be called complete without actual implementations. Temporary salary amounts are not payroll calculations.
